<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oco.mapper.ReplyMapper">
	<select id="getTotal">
		select count(*) from replies where boardNum=#{boardNum}
	</select>
	<select id="getRecentReply">
		<![CDATA[ 
			select count(*) from replies where boardNum=#{boardNum} and timestampdiff(HOUR,regdate,now())<1 
		]]>
	</select>
</mapper>
