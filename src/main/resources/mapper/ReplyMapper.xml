<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oco.mapper.ReplyMapper">
	<select id="getTotal">
		select count(*) from replies where board_num=#{board_num}
	</select>
	<select id="getRecentReply">
		<![CDATA[ 
			select count(*) from replies where board_num=#{board_num} and timestampdiff(HOUR,reg_date,now())<1 
		]]>
	</select>
</mapper>
